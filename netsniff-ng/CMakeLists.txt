INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/${NETSNIFF-NG}/include)
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/${NET-NG}/include)
ADD_EXECUTABLE(${NETSNIFF-NG} netsniff-ng.c config.c misc.c server.c system.c)
TARGET_LINK_LIBRARIES (${NETSNIFF-NG} ${NET-NG})
